# Copy to .env and fill in. .env is gitignored — never commit the key.
SAHARA_API_KEY=
# SAUTI_MODE=cloud | offline   (default: cloud if key set, else offline)
# SAUTI_PACK=pcm-yo-NG
# SAUTI_DB=data/ledger.db
